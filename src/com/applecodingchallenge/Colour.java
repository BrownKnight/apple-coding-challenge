package com.applecodingchallenge;

/**
 * Colours that can be stored in the InMemoryStore.
 *
 * @implNote The order here matters, the lower the position in the enum,
 * the higher priority the colour has.
 */
public enum Colour {
    YELLOW,
    RED,
    GREEN,
    BLUE,
    GREY
}
